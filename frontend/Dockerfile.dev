# Development Dockerfile for Vue 3 Frontend
FROM node:20-slim

# Install pnpm globally
RUN npm install -g pnpm@10.28.0

WORKDIR /app

EXPOSE 8100

# No COPY here - we use bind mount for hot reload
# The command is defined in docker-compose.yml